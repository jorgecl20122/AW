<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Gestión de Vehículos</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/vista.css">
</head>
<body data-user-role="<%= usuario ? usuario.rol : 'empleado' %>">

<header class="header d-flex align-items-center p-3">
  <img src="/img/logo.png" alt="Logo Empresa" class="logo_img me-4">
  <div>
    <h4 class="mb-0 fw-bold">EcoFlota</h4>
    <small class="text-muted fw-bold"><em>Plataforma web de Gestión de Flotas de Vehículos Eléctricos</em></small>
  </div>
</header>

  <!-- Barra de navegación superior -->
<nav class="navbar navbar-expand-lg">
  <div class="container-fluid">
    <div class="d-flex align-items-center">
      <a href="/empleado/vista_empleado" class="btn-custom mx-2 nav-link" data-route="/empleado/vista_empleado"><i class="bi bi-house"></i><strong>Inicio</strong> </a>
      <a href="/empleado/mis_reservas" class="btn-custom mx-2 nav-link" data-route="/empleado/mis_reservas"><i class="bi bi-calendar-check "></i><strong> Reservas</strong> </a>
      <a href="#" class="btn-custom mx-2 nav-link" data-bs-toggle="modal" data-bs-target="#configuradorModal"><i class="bi bi-person-wheelchair"></i><strong>Accesibilidad</strong></a>
    </div>

    <div class="d-flex align-items-center ms-auto">
      <a href="/usuario/logout" class="btn btn-custom mx-2">
        <i class="bi bi-box-arrow-right"></i> <strong>Cerrar sesión</strong>
      </a>
    </div>
  </div>
</nav>

<!-- para lateral con las estadisticas y el usuario abajo-->
<div class="d-flex">
  <!-- SIDEBAR -->
  <aside class="sidebar p-3">

    <!-- Información del usuario (PERFIL) -->
    <div class="user-info mt-auto d-flex flex-column align-items-center text-center gap-2">
      <% if (usuario) { %>
        <img id="userAvatar" src="<%= usuario.avatar %>" alt="Avatar" class="rounded-circle">
        <div>
          <p class="mb-0 fw-bold"><%= usuario.nombre_completo %></p>
          <small class="text-muted"><%= usuario.email %></small>
        </div>
      <% } else { %>
        <img id="userAvatar" src="/img/default-avatar.png" alt="Avatar" class="rounded-circle">
        <div>
          <p class="text-danger">Usuario no cargado</p>
        </div>
      <% } %>
    </div>
  </aside>

  <!-- MAIN CONTENT -->
  <main class="flex-grow-1 p-4">
    <div class="d-flex align-items-center mb-4">
      <h2 class="fw-bold">Gestión de Reservas</h2>
    </div>

    <!-- Tabla dinámica de vehículos -->
    <div id="listaReservas" class="table-responsive">
      <% if (locals.error) { %>
        <div class="alert alert-danger text-center" role="alert">
          <i class="bi bi-exclamation-triangle-fill me-2"></i>
          <%= error %>
        </div>
      <% } else if (!vehiculos || vehiculos.length === 0) { %>
        <div class="alert alert-info text-center" role="alert">
          <i class="bi bi-info-circle-fill me-2"></i>
          No hay vehículos disponibles en tu concesionario.
        </div>
      <% } else { %>
        <table class="table table-striped table-hover align-middle">
          <thead class="table-secondary">
            <tr>
              <th>Matrícula</th>
              <th>Marca</th>
              <th>Modelo</th>
              <th>Año</th>
              <th>Plazas</th>
              <th>Autonomía</th>
              <th>Color</th>
              <th>Imagen</th>
              <th>Acción</th>
            </tr>
          </thead>
          <tbody>
            <% vehiculos.forEach(function(v) { %>
              <tr data-id="<%= v.id %>" id="vehiculo-<%= v.id %>">
                <td><%= v.matricula %></td>
                <td><%= v.marca %></td>
                <td><%= v.modelo %></td>
                <td><%= v.anio_matriculacion %></td>
                <td><%= v.numero_plazas %></td>
                <td><%= v.autonomia_km %> km</td>
                <td><%= v.color %></td>
                <td>
                  <% if (v.imagen) { %>
                    <img src="<%= v.imagen %>" alt="<%= v.marca %> <%= v.modelo %>" class="vehiculo-img">
                  <% } else { %>
                    <span class="text-muted">-</span>
                  <% } %>
                </td>
                <td>
                 <button class="btn btn-reservar"
                        data-bs-toggle="modal"
                        data-bs-target="#modalReserva"
                        data-vehiculo-id="<%= v.id %>"
                        data-matricula="<%= v.matricula %>"
                        data-marca="<%= v.marca %>"
                        data-modelo="<%= v.modelo %>">
                    <i class="bi bi-calendar-check"></i><strong> Reservar</strong>
                </button>
                </td>
              </tr>
            <% }); %>
          </tbody>
        </table>
      <% } %>
    </div>
  </main>
</div> 

<!-- MODALES -->

<!-- Modal de Reserva con Time Picker -->
<div class="modal fade" id="modalReserva" tabindex="-1" aria-labelledby="modalReservaLabel">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content shadow-lg rounded-4 border-0">
      
      <!-- Header -->
      <div class="modal-header text-white rounded-top-4">
        <h5 class="modal-title fw-bold" id="modalReservaLabel">
          <i class="bi bi-calendar-check me-2"></i>
          Nueva Reserva
        </h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Cerrar"></button>
      </div>
      
      <!-- Body -->
      <div class="modal-body p-4">
        
        <!-- Información del vehículo -->
        <div class="alert alert-info d-flex align-items-center mb-4">
          <i class="bi bi-car-front-fill fs-4 me-3"></i>
          <div>
            <strong>Vehículo seleccionado:</strong><br>
            <span id="vehiculoInfoReserva" class="text-dark"></span>
          </div>
        </div>

        <form id="formReserva">
          <input type="hidden" id="vehiculoIdReserva" name="vehiculo_id">

          <!-- Fecha y Hora de Inicio -->
          <div class="mb-4">
            <label class="form-label fw-bold">
              <i class="bi bi-calendar-event me-2"></i>Inicio de la reserva
            </label>
            
            <div class="row g-2">
              <div class="col-7">
                <input type="date" class="form-control" id="fechaInicioReserva" required>
                <small class="text-muted">Fecha</small>
              </div>
              <div class="col-5">
                <input type="time" class="form-control" id="horaInicioReserva" required>
                <small class="text-muted">Hora</small>
              </div>
            </div>
          </div>

          <!-- Fecha y Hora de Fin -->
          <div class="mb-4">
            <label class="form-label fw-bold">
              <i class="bi bi-calendar-x me-2"></i>Fin de la reserva
            </label>
            
            <div class="row g-2">
              <div class="col-7">
                <input type="date" class="form-control" id="fechaFinReserva" required>
                <small class="text-muted">Fecha</small>
              </div>
              <div class="col-5">
                <input type="time" class="form-control" id="horaFinReserva" required>
                <small class="text-muted">Hora</small>
              </div>
            </div>
          </div>

          <!-- Resumen de duración (calculado automáticamente) -->
          <div id="resumenDuracion" class="alert alert-secondary" style="display: none;">
            <strong><i class="bi bi-clock me-2"></i>Duración:</strong>
            <span id="textoDuracion"></span>
          </div>

        </form>
      </div>
      
      <!-- Footer -->
      <div class="modal-footer rounded-bottom-4">
        <button type="button" class="btn btn-primary" data-bs-dismiss="modal">
          <i class="bi bi-x-circle me-1"></i> Cancelar
        </button>
        <button type="button" class="btn btn-primary" id="btnGuardarReserva">
          <i class="bi bi-check-circle me-1"></i> Confirmar Reserva
        </button>
      </div>
    </div>
  </div>
</div>


    <!-- Modal de Configuración de Accesibilidad -->
<div class="modal fade" id="configuradorModal" tabindex="-1" aria-labelledby="configuradorLabel">
  <div class="modal-dialog modal-xl modal-dialog-scrollable modal-dialog-centered">
    <div class="modal-content shadow-lg rounded-4 border-0">
      
      <!-- Header -->
      <div class="modal-header text-white rounded-top-4">
        <h5 class="modal-title fw-bold" id="configuradorLabel">
          <i class="bi bi-universal-access-circle me-2"></i> 
          Configuración de Accesibilidad
        </h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Cerrar"></button>
      </div>
      
      <!-- Body con Tabs -->
      <div class="modal-body p-4">
        <ul class="nav nav-tabs mb-3" id="accesibilidadTabs" role="tablist">
          <li class="nav-item" role="presentation">
            <button class="nav-link active" id="visual-tab" data-bs-toggle="tab" 
                    data-bs-target="#visual" type="button" role="tab" aria-controls="visual">
              <i class="bi bi-eye-fill me-1"></i> Visual
            </button>
          </li>
          <li class="nav-item" role="presentation">
            <button class="nav-link" id="teclado-tab" data-bs-toggle="tab" 
                    data-bs-target="#teclado" type="button" role="tab" aria-controls="teclado">
              <i class="bi bi-keyboard-fill me-1"></i> Atajos de Teclado
            </button>
          </li>
          <li class="nav-item" role="presentation">
            <button class="nav-link" id="navegacion-tab" data-bs-toggle="tab" 
                    data-bs-target="#navegacion" type="button" role="tab" aria-controls="navegacion">
              <i class="bi bi-signpost-split-fill me-1"></i> Navegación
            </button>
          </li>
        </ul>

        <form id="form-configurador">
          <div class="tab-content" id="accesibilidadTabContent">
            
            <!-- ========================================== -->
            <!-- TAB 1: CONFIGURACIÓN VISUAL -->
            <!-- ========================================== -->
            <div class="tab-pane fade show active" id="visual" role="tabpanel" aria-labelledby="visual-tab">
              
              <!-- Tamaño de Fuente -->
              <div class="mb-4">
                <h6 class="fw-bold">
                  <i class="bi bi-fonts me-2"></i>Tamaño de texto
                </h6>
                
                <div class="row">
                  <div class="col-md-6 mb-3">
                    <label for="fuente-titulo" class="form-label">Títulos</label>
                    <input type="range" class="form-range" id="fuente-titulo" 
                           min="1.5" max="4" step="0.1" value="2.5">
                    <small class="text-muted">Ajusta el tamaño de los títulos</small>
                  </div>
                  
                  <div class="col-md-6 mb-3">
                    <label for="fuente-texto" class="form-label">Texto general</label>
                    <input type="range" class="form-range" id="fuente-texto" 
                           min="0.8" max="2" step="0.1" value="1">
                    <small class="text-muted">Ajusta el tamaño del texto normal</small>
                  </div>
                </div>
              </div>

              <hr>

              <!-- Colores -->
              <div class="mb-4">
                <h6 class="fw-bold">
                  <i class="bi bi-palette-fill me-2"></i>Colores
                </h6>
                
                <div class="row">
                  <div class="col-md-4 mb-3">
                    <label for="color-titulos" class="form-label">Color de títulos</label>
                    <input type="color" class="form-control form-control-color w-100" 
                           id="color-titulos" value="#31a1a9">
                  </div>
                  
                  <div class="col-md-4 mb-3">
                    <label for="color-texto" class="form-label">Color del texto</label>
                    <input type="color" class="form-control form-control-color w-100" 
                           id="color-texto" value="#000000">
                  </div>
                  
                  <div class="col-md-4 mb-3">
                    <label for="fondo-seccion" class="form-label">Color de fondo</label>
                    <input type="color" class="form-control form-control-color w-100" 
                           id="fondo-seccion" value="#f4f4f4">
                  </div>
                </div>
              </div>

              <hr>

              <!-- Opciones adicionales -->
              <div class="mb-3">
                <h6 class="fw-bold">
                  <i class="bi bi-gear-fill me-2"></i>Opciones visuales
                </h6>
                
                <div class="form-check form-switch mb-2">
                  <input class="form-check-input" type="checkbox" id="subrayado-enlaces">
                  <label class="form-check-label" for="subrayado-enlaces">
                    Subrayar todos los enlaces
                  </label>
                </div>
                
                <div class="form-check form-switch mb-2">
                  <input class="form-check-input" type="checkbox" id="modo-contraste">
                  <label class="form-check-label" for="modo-contraste">
                    Modo alto contraste
                  </label>
                </div>
                
                <div class="form-check form-switch">
                  <input class="form-check-input" type="checkbox" id="modo-daltonismo">
                  <label class="form-check-label" for="modo-daltonismo">
                    Modo daltonismo
                  </label>
                </div>
              </div>
            </div>

            <!-- ========================================== -->
            <!-- TAB 2: ATAJOS DE TECLADO -->
            <!-- ========================================== -->
            <div class="tab-pane fade" id="teclado" role="tabpanel" aria-labelledby="teclado-tab">
              
              <div class="alert alert-info d-flex align-items-center">
                <i class="bi bi-info-circle-fill me-3 fs-4"></i>
                <div>
                  <strong>Personaliza tus atajos de teclado</strong><br>
                  <small>Puedes modificar o crear nuevos atajos para navegar más rápido por la aplicación.</small>
                </div>
              </div>

              <div class="d-flex justify-content-between align-items-center mb-3">
                <h6 class="mb-0 fw-bold">Atajos configurados</h6>
                <button type="button" class="btn btn-sm btn-primary" id="btnAnadirAtajo">
                  <i class="bi bi-plus-circle me-1"></i> Añadir atajo
                </button>
              </div>

              <div class="table-responsive">
                <table class="table table-hover table-bordered align-middle" id="tablaAtajos">
                  <thead class="table-light">
                    <tr>
                      <th style="width: 25%;">Combinación</th>
                      <th style="width: 50%;">Acción</th>
                      <th style="width: 25%;" class="text-center">Opciones</th>
                    </tr>
                  </thead>
                  <tbody>
                    <!-- Los atajos se cargan dinámicamente desde JavaScript -->
                  </tbody>
                </table>
              </div>

              <div class="alert alert-warning mt-3">
                <i class="bi bi-exclamation-triangle-fill me-2"></i>
                <strong>Nota:</strong> Los atajos predefinidos no pueden ser editados o eliminados.
              </div>
            </div>

            <!-- ========================================== -->
            <!-- TAB 3: NAVEGACIÓN -->
            <!-- ========================================== -->
            <div class="tab-pane fade" id="navegacion" role="tabpanel" aria-labelledby="navegacion-tab">
              
              <div class="alert alert-success d-flex align-items-center">
                <i class="bi bi-lightbulb-fill me-3 fs-4"></i>
                <div>
                  <strong>Mejora tu experiencia de navegación</strong><br>
                  <small>Configura el orden de tabulación y activa atajos rápidos para saltar entre secciones.</small>
                </div>
              </div>

              <!-- Orden de Tabulación -->
              <div class="mb-4">
                <h6 class="fw-bold">
                  <i class="bi bi-arrow-down-up me-2"></i>Orden de tabulación
                </h6>
                <p class="text-muted small mb-3">
                  Define cómo se navega entre elementos al presionar la tecla Tab.
                </p>

                <select class="form-select" id="orden-tabulacion">
                  <option value="default">Predeterminado (orden del DOM)</option>
                  <option value="optimizado">Optimizado (prioridad lógica)</option>
                  <option value="personalizado">Personalizado (saltos por sección)</option>
                </select>

                <div class="mt-3 p-3 bg-light rounded">
                  <small class="d-block mb-2">
                    <strong>Predeterminado:</strong> Sigue el orden natural del HTML.
                  </small>
                  <small class="d-block mb-2">
                    <strong>Optimizado:</strong> Menú → Botones principales → Formularios → Tablas → Enlaces.
                  </small>
                </div>
              </div>

              <hr>

          </div>
        </form>
      </div>
      
    </div>
     <!-- Footer -->
      <div class="modal-footer rounded-bottom-4">
        <button type="button" class="btn btn-primary" data-bs-dismiss="modal">
          <i class="bi bi-x-circle me-1"></i> Cerrar
        </button>
        <button type="button" class="btn btn-primary" id="btnRestaurar">
          <i class="bi bi-arrow-counterclockwise me-1"></i> Restaurar valores
        </button>
      </div>
  </div>
</div>

<!-- ========================================== -->
<!-- MODAL PARA AÑADIR/EDITAR ATAJOS (INTUITIVO) -->
<!-- ========================================== -->
<div class="modal fade" id="modalAtajo" tabindex="-1" aria-labelledby="modalAtajoLabel">
  <div class="modal-dialog modal-dialog-centered modal-lg">
    <div class="modal-content shadow-lg rounded-4 border-0">
      
      <!-- Header -->
      <div class="modal-header text-white rounded-top-4">
        <h5 class="modal-title fw-bold" id="modalAtajoLabel">
          <i class="bi bi-keyboard me-2"></i>
          <span id="tituloModalAtajo">Añadir Atajo de Teclado</span>
        </h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Cerrar"></button>
      </div>
      
      <!-- Body -->
      <div class="modal-body p-4">
        
        <!-- PASO 1: Combinación de teclas -->
        <div class="mb-4">
          <div class="d-flex align-items-center mb-2">
            <span class="badge bg-primary me-2">1</span>
            <label for="inputCombinacion" class="form-label fw-bold mb-0">
              Presiona la combinación de teclas que deseas usar
            </label>
          </div>
          <input type="text" class="form-control form-control-lg text-center fw-bold" 
                 id="inputCombinacion" 
                 placeholder="Presiona las teclas..." 
                 readonly>
          <small class="form-text text-muted d-block mt-2 text-center">
            <i class="bi bi-info-circle me-1"></i>
            Ejemplo: mantén <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>K</kbd>
          </small>
        </div>

        <hr>

        <!-- PASO 2: Tipo de acción -->
        <div class="mb-4">
          <div class="d-flex align-items-center mb-2">
            <span class="badge bg-primary me-2">2</span>
            <label class="form-label fw-bold mb-0">
              ¿Qué quieres que haga este atajo?
            </label>
          </div>
          
          <div class="row g-3">
            <div class="col-md-6">
              <div class="card h-100 tipo-accion-card" data-tipo="navegacion">
                <div class="card-body text-center cursor-pointer">
                  <i class="bi bi-compass fs-1 text-primary"></i>
                  <h6 class="mt-2 mb-0">Ir a una página</h6>
                  <small class="text-muted">Navegar a otra sección</small>
                </div>
              </div>
            </div>
            
            <div class="col-md-6">
              <div class="card h-100 tipo-accion-card" data-tipo="click">
                <div class="card-body text-center cursor-pointer">
                  <i class="bi bi-hand-index fs-1 text-success"></i>
                  <h6 class="mt-2 mb-0">Hacer clic</h6>
                  <small class="text-muted">Activar un botón</small>
                </div>
              </div>
            </div>
          </div>
          
          <input type="hidden" id="tipoAccionSeleccionado">
        </div>

        <hr>

        <!-- PASO 3: Selección del destino (se muestra después de elegir tipo) -->
        <div class="mb-4" id="paso3Destino" style="display:none;">
          <div class="d-flex align-items-center mb-3">
            <span class="badge bg-primary me-2">3</span>
            <label class="form-label fw-bold mb-0" id="labelPaso3">
              Selecciona el destino
            </label>
          </div>

          <!-- Lista de páginas disponibles (para navegación) -->
          <div id="listaPaginas" style="display:none;">
            <div class="list-group" id="grupoEnlaces">
              <!-- Se llena dinámicamente con JavaScript -->
            </div>
          </div>

          <!-- Lista de botones (para click) -->
          <div id="listaBotones" style="display:none;">
            <div class="input-group mb-3">
              <span class="input-group-text">
                <i class="bi bi-search"></i>
              </span>
              <input type="text" class="form-control" id="buscarBoton" 
                     placeholder="Buscar botón...">
            </div>
            <div class="list-group" id="grupoBotones" style="max-height: 300px; overflow-y: auto;">
              <!-- Se llena dinámicamente con JavaScript -->
            </div>
          </div>

          <input type="hidden" id="destinoSeleccionado">
          <input type="hidden" id="descripcionDestino">
        </div>

        <!-- Resumen -->
        <div id="resumenAtajo" class="alert alert-success" style="display:none;">
          <strong><i class="bi bi-check-circle me-2"></i>Resumen de tu atajo:</strong>
          <div class="mt-2">
            <strong>Combinación:</strong> <span id="resumenCombinacion"></span><br>
            <strong>Acción:</strong> <span id="resumenAccion"></span>
          </div>
        </div>

      </div>
      
      <!-- Footer -->
      <div class="modal-footer rounded-bottom-4">
        <button type="button" class="btn btn-primary" data-bs-dismiss="modal">
          <i class="bi bi-x-circle me-1"></i> Cancelar
        </button>
        <button type="button" class="btn btn-primary" id="btnGuardarAtajo" disabled>
          <i class="bi bi-save me-1"></i> Guardar atajo
        </button>
      </div>
    </div>
  </div>
</div>

<!-- Scripts -->
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script src="/javascripts/script_empleado.js"></script>
<script src="/javascripts/accesibilidad.js"></script>

</body>
</html>
